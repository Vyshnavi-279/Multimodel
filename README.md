# ⚡ Multi-Model Comparison Tool

A Python application that sends a single prompt to multiple LLMs via OpenRouter and compares their answers, latency, and estimated cost side-by-side. 

Features both a Terminal interface and a Streamlit Web Dashboard.

## 🛠️ Setup & Installation

1. **Clone the repo and create a virtual environment:**
   ```bash
   git clone <your-repo-url>
   cd multimodel-app
   python -m venv .venv
   source .venv/bin/activate  # On Windows use: .venv\Scripts\activate